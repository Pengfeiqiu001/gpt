# ==== Required ====
OPENAI_API_KEY=sk-xxxx

# ==== Optional (recommended) ====
# Allowed front-end origins, comma separated. Example: https://your.pages.dev,https://app.example.com
CORS_ORIGINS=*
# Simple per-request token for your app's users (set in front-end). Example: set to a UUID.
APP_TOKEN=changeme-demo-token

# Rate limiting (per IP)
RATE_LIMIT_POINTS=120
RATE_LIMIT_DURATION=60

# Model + cost controls
DEFAULT_MODEL=gpt-4o-mini
MAX_TOKENS=512
TEMPERATURE=0.7
OPENAI_BASE_URL=https://api.openai.com
PORT=3000
